<%layout("layouts/boilerplate")%>

    <div style="word-wrap:break-word" class=" word-wrap mt-5 row col-6 offset-3 alert alert-danger" role="alert">
        <h4 class="alert-heading"><%=err.message%></h4>
        <hr>
        <% if(process.env.NODE_ENV !== "production"){%>
            <p><%=err.stack%></p>
            <%}%>
    </div>